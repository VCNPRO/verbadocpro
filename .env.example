# Vertex AI Configuration - Europe (Belgium)
# Required for processing healthcare data in compliance with GDPR

# Your Google Cloud Project ID
VITE_GEMINI_PROJECT_ID=your-project-id-here

# Alternative project ID variable (used by Vertex AI SDK)
GOOGLE_CLOUD_PROJECT=your-project-id-here

# Google Cloud Service Account Credentials (JSON)
# For local development: path to service account JSON file
# For Vercel: paste the entire JSON content as environment variable
GOOGLE_APPLICATION_CREDENTIALS=./path/to/service-account-key.json

# Vertex AI Region (DO NOT CHANGE - must be Europe for GDPR compliance)
VITE_GEMINI_LOCATION=europe-west1

# Vercel KV (Required for Queue System)
# Get from: Vercel Dashboard → Storage → Create KV Database
KV_URL=https://xxx-xxx.kv.vercel-storage.com
KV_REST_API_URL=https://xxx-xxx.kv.vercel-storage.com
KV_REST_API_TOKEN=xxxxxxxxxxxxx
KV_REST_API_READ_ONLY_TOKEN=xxxxxxxxxxxxx

# Cron Secret (Required for Queue Processing Security)
# Generate with: openssl rand -hex 32
CRON_SECRET=your-random-secret-here

# Authentication (Required for user login/registration)
# Generate with: openssl rand -hex 32
JWT_SECRET=your-jwt-secret-here

# Vercel Postgres (Required for user database)
# Get from: Vercel Dashboard → Storage → Postgres Database
POSTGRES_URL=postgres://default:xxx@xxx.postgres.vercel-storage.com:5432/verceldb
POSTGRES_PRISMA_URL=postgres://default:xxx@xxx.postgres.vercel-storage.com:5432/verceldb?pgbouncer=true&connect_timeout=15
POSTGRES_URL_NON_POOLING=postgres://default:xxx@xxx.postgres.vercel-storage.com:5432/verceldb
POSTGRES_USER=default
POSTGRES_HOST=xxx.postgres.vercel-storage.com
POSTGRES_PASSWORD=xxx
POSTGRES_DATABASE=verceldb
