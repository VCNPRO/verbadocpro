{
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 800
    }
  },
  "crons": [
    {
      "path": "/api/process-queue",
      "schedule": "* * * * *"
    }
  ],
  "rewrites": [{ "source": "/((?!api).*)", "destination": "/index.html" }],
  "devCommand": "vite --port $PORT"
}