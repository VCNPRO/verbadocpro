# ⚠️ IMPORTANT: Copy this file to .env.local and fill in your values
# Never commit .env.local to git (it's in .gitignore)

# JWT Secret for authentication tokens
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-secret-key-here-min-32-characters

# PostgreSQL Database (provided by Vercel)
# POSTGRES_URL is automatically set by Vercel - no need to add it here unless testing locally

# Optional: Vercel KV (Redis) - Currently not used
# KV_URL=""
# KV_REST_API_URL=""
# KV_REST_API_TOKEN=""
# KV_REST_API_READ_ONLY_TOKEN=""
